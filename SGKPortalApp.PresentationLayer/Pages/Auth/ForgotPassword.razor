@page "/auth/forgot-password"
@layout EmptyLayout
@using SGKPortalApp.BusinessObjectLayer.DTOs.Request.Auth

<PageTitle>Şifremi Unuttum - SGK Portal</PageTitle>

<!-- Content -->
<div class="container-xxl">
    <div class="authentication-wrapper authentication-basic container-p-y">
        <div class="authentication-inner">
            <!-- Forgot Password Card -->
            <div class="card">
                <div class="card-body">
                    <!-- Logo -->
                    <div class="app-brand justify-content-center mb-4">
                        <a href="/" class="app-brand-link gap-2">
                            <SgkLogo Width="120" Height="48" />
                            <span class="app-brand-text demo text-body fw-bold fs-4">SGK Portal</span>
                        </a>
                    </div>
                    <!-- /Logo -->
                    
                    <h4 class="mb-2">Şifre Sıfırlama 🔒</h4>
                    <p class="mb-4">Kimliğinizi doğrulamak için aşağıdaki bilgileri giriniz</p>

                    @if (!string.IsNullOrEmpty(errorMessage))
                    {
                        <div class="alert alert-danger alert-dismissible" role="alert">
                            <button type="button" class="btn-close" @onclick="() => errorMessage = null"></button>
                            <i class="bx bx-error-circle me-2"></i>
                            @errorMessage
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(successMessage))
                    {
                        <div class="alert alert-success alert-dismissible" role="alert">
                            <button type="button" class="btn-close" @onclick="() => successMessage = null"></button>
                            <i class="bx bx-check-circle me-2"></i>
                            @successMessage
                        </div>
                    }

                    <EditForm Model="@verifyModel" OnValidSubmit="HandleVerify" class="mb-3">
                        <DataAnnotationsValidator />
                        
                        <!-- TC Kimlik No -->
                        <div class="mb-3">
                            <label for="tcKimlikNo" class="form-label">TC Kimlik No</label>
                            <InputText @bind-Value="verifyModel.TcKimlikNo" 
                                       class="form-control" 
                                       id="tcKimlikNo" 
                                       placeholder="11 haneli TC Kimlik No" 
                                       maxlength="11"
                                       autofocus />
                            <ValidationMessage For="@(() => verifyModel.TcKimlikNo)" class="text-danger" />
                        </div>

                        <!-- Sicil No -->
                        <div class="mb-3">
                            <label for="sicilNo" class="form-label">Sicil No</label>
                            <InputNumber @bind-Value="verifyModel.SicilNo" 
                                         class="form-control" 
                                         id="sicilNo" 
                                         placeholder="Sicil numaranızı giriniz" />
                            <ValidationMessage For="@(() => verifyModel.SicilNo)" class="text-danger" />
                        </div>

                        <!-- Doğum Tarihi -->
                        <div class="mb-3">
                            <label for="dogumTarihi" class="form-label">Doğum Tarihi</label>
                            <InputDate @bind-Value="verifyModel.DogumTarihi" 
                                       class="form-control" 
                                       id="dogumTarihi" />
                            <ValidationMessage For="@(() => verifyModel.DogumTarihi)" class="text-danger" />
                        </div>

                        <!-- Email -->
                        <div class="mb-3">
                            <label for="email" class="form-label">Email Adresi</label>
                            <InputText @bind-Value="verifyModel.Email" 
                                       type="email"
                                       class="form-control" 
                                       id="email" 
                                       placeholder="ornek@sgk.gov.tr" />
                            <ValidationMessage For="@(() => verifyModel.Email)" class="text-danger" />
                        </div>

                        <!-- Doğrula Butonu -->
                        <div class="mb-3">
                            <button class="btn btn-primary d-grid w-100" type="submit" disabled="@isLoading">
                                @if (isLoading)
                                {
                                    <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                                    <span>Doğrulanıyor...</span>
                                }
                                else
                                {
                                    <span>Kimliğimi Doğrula</span>
                                }
                            </button>
                        </div>

                        <div class="text-center">
                            <a href="/auth/login" class="d-flex align-items-center justify-content-center">
                                <i class="bx bx-chevron-left scaleX-n1-rtl bx-sm"></i>
                                Giriş sayfasına dön
                            </a>
                        </div>
                    </EditForm>
                </div>
            </div>
            <!-- /Forgot Password Card -->
        </div>
    </div>
</div>
<!-- / Content -->

