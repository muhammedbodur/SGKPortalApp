@page "/pdks/izin/yeni-talep"
@using SGKPortalApp.BusinessObjectLayer.DTOs.Request.PdksIslemleri
@using SGKPortalApp.BusinessObjectLayer.Enums.PdksIslemleri
@using SGKPortalApp.Common.Extensions
@using SGKPortalApp.PresentationLayer.Components.Permission
@inherits SGKPortalApp.PresentationLayer.Components.Base.FieldPermissionPageBase

<PageTitle>Yeni İzin/Mazeret Talebi</PageTitle>

@if (!CanViewPage)
{
    <div class="alert alert-danger" role="alert">
        <i class="bx bx-lock-alt me-2"></i>
        Bu sayfayı görüntüleme yetkiniz bulunmuyor.
    </div>
}
else
{
    <FieldPermissionScope PageKey="@ResolvedPermissionKey" IsEditMode="false">
        <div class="container-fluid">
    <!-- Header -->
    <div class="row mb-3">
        <div class="col">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/pdks/izin/taleplerim">Taleplerim</a></li>
                    <li class="breadcrumb-item active">Yeni Talep</li>
                </ol>
            </nav>
            <h3>📝 Yeni İzin/Mazeret Talebi</h3>
            <p class="text-muted">İzin veya mazeret talebinizi oluşturun</p>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-8">
            <div class="card">
                <div class="card-body">
                    <EditForm Model="@Request" OnValidSubmit="HandleSubmit">
                        <DataAnnotationsValidator />

                        <!-- İzin/Mazeret Türü -->
                        <div class="mb-3">
                            <label class="form-label">İzin/Mazeret Türü <span class="text-danger">*</span></label>
                            <select class="form-select @(ShowTurError ? "is-invalid" : "")"
                                    value="@SelectedTuru" @onchange="OnTuruChanged">
                                <option value="">Seçiniz...</option>
                                @foreach (var izinTuru in IzinTurleri)
                                {
                                    <option value="@izinTuru.IzinMazeretTuruId">@izinTuru.TuruAdi @izinTuru.Aciklama</option>
                                }
                            </select>
                            @if (ShowTurError)
                            {
                                <div class="invalid-feedback">İzin/Mazeret türü seçmelisiniz</div>
                            }
                            @if (IzinTurleri.Any())
                            {
                                <small class="form-text text-muted">@IzinTurleri.Count adet izin türü mevcut</small>
                            }
                        </div>

                        @if (SelectedTuru.HasValue && SelectedIzinTuru != null)
                        {
                            @if (!SelectedIzinTuru.PlanliIzinMi)
                            {
                                <!-- Mazeret için: Tarih + Saat Dilimi -->
                                <div class="mb-3">
                                    <label class="form-label">Mazeret Tarihi <span class="text-danger">*</span></label>
                                    <input type="date" class="form-control" @bind="Request.MazeretTarihi" />
                                    <ValidationMessage For="@(() => Request.MazeretTarihi)" />
                                </div>

                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Başlangıç Saati <span class="text-danger">*</span></label>
                                        <input type="time" class="form-control"
                                               value="@BaslangicSaatiStr"
                                               @onchange="@(e => BaslangicSaatiStr = e.Value?.ToString() ?? "")" />
                                        <ValidationMessage For="@(() => Request.BaslangicSaati)" />
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Bitiş Saati <span class="text-danger">*</span></label>
                                        <input type="time" class="form-control"
                                               value="@BitisSaatiStr"
                                               @onchange="@(e => BitisSaatiStr = e.Value?.ToString() ?? "")" />
                                        <ValidationMessage For="@(() => Request.BitisSaati)" />
                                    </div>
                                </div>
                                <small class="form-text text-muted">Mazeret gösterdiğiniz saat aralığını seçin</small>
                            }
                            else
                            {
                                <!-- İzin için: Başlangıç + Bitiş Tarihi -->
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Başlangıç Tarihi <span class="text-danger">*</span></label>
                                        <input type="date" class="form-control"
                                               value="@(Request.BaslangicTarihi?.ToString("yyyy-MM-dd"))"
                                               @onchange="@(e => { Request.BaslangicTarihi = DateTime.TryParse(e.Value?.ToString(), out var d) ? d : (DateTime?)null; CalculateDays(); })" />
                                        <ValidationMessage For="@(() => Request.BaslangicTarihi)" />
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Bitiş Tarihi <span class="text-danger">*</span></label>
                                        <input type="date" class="form-control"
                                               value="@(Request.BitisTarihi?.ToString("yyyy-MM-dd"))"
                                               @onchange="@(e => { Request.BitisTarihi = DateTime.TryParse(e.Value?.ToString(), out var d) ? d : (DateTime?)null; CalculateDays(); })" />
                                        <ValidationMessage For="@(() => Request.BitisTarihi)" />
                                    </div>
                                </div>

                                @if (ToplamGun > 0)
                                {
                                    <div class="alert alert-info">
                                        <i class="bx bx-info-circle"></i> <strong>Toplam Gün:</strong> @ToplamGun gün
                                    </div>
                                }
                            }

                            <!-- Açıklama -->
                            <div class="mb-3">
                                <label class="form-label">Açıklama</label>
                                <textarea class="form-control" rows="4" @bind="Request.Aciklama"
                                          placeholder="İzin/Mazeret sebebinizi açıklayın..."></textarea>
                                <ValidationMessage For="@(() => Request.Aciklama)" />
                            </div>

                            <!-- Onaycı Seçimi (Opsiyonel - Sistem otomatik atayabilir) -->
                            <div class="alert alert-info mb-3">
                                <i class="bx bx-info-circle"></i>
                                <strong>Onaycı Bilgisi:</strong>
                                @if (!RequiresSecondApprover())
                                {
                                    <span>Bu izin türü için sadece 1. onaycı onayı yeterlidir. Boş bırakırsanız sistem otomatik atayacaktır.</span>
                                }
                                else
                                {
                                    <span>Bu izin türü için 1. VE 2. onaycı sırasıyla onaylar. Boş bırakırsanız sistem otomatik atayacaktır.</span>
                                }
                            </div>

                            @if (IsLoadingApprovers)
                            {
                                <div class="text-center mb-3">
                                    <span class="spinner-border spinner-border-sm me-2"></span>
                                    Onaycılar yükleniyor...
                                </div>
                            }
                            else
                            {
                                <!-- Birinci Onaycı -->
                                <div class="mb-3">
                                    <label class="form-label">
                                        1. Onaycı
                                        @if (SelectedIzinTuru.BirinciOnayciGerekli)
                                        {
                                            <span class="badge bg-warning ms-2">Gerekli</span>
                                        }
                                    </label>
                                    <select class="form-select" @bind="SelectedBirinciOnayci">
                                        <option value="">Sistem otomatik atayacak</option>
                                        @foreach (var onayci in AvailableApprovers)
                                        {
                                            <option value="@onayci.TcKimlikNo" disabled="@(onayci.TcKimlikNo == SelectedIkinciOnayci)">
                                                @onayci.AdSoyad (@onayci.DepartmanAdi@(onayci.ServisAdi != null ? " - " + onayci.ServisAdi : "")) - @onayci.UnvanAdi
                                            </option>
                                        }
                                    </select>
                                    <small class="form-text text-muted">
                                        @if (AvailableApprovers.Any())
                                        {
                                            <span>@AvailableApprovers.Count adet onaycı bulundu</span>
                                        }
                                        else
                                        {
                                            <span class="text-warning">⚠️ Hiç onaycı bulunamadı</span>
                                        }
                                    </small>
                                </div>

                                <!-- İkinci Onaycı - Sadece gerekli ise göster -->
                                @if (RequiresSecondApprover())
                                {
                                    <div class="mb-3">
                                        <label class="form-label">
                                            2. Onaycı
                                            @if (SelectedIzinTuru.IkinciOnayciGerekli)
                                            {
                                                <span class="badge bg-warning ms-2">Gerekli</span>
                                            }
                                        </label>
                                        <select class="form-select" @bind="SelectedIkinciOnayci">
                                            <option value="">Sistem otomatik atayacak</option>
                                            @foreach (var onayci in AvailableApprovers)
                                            {
                                                <option value="@onayci.TcKimlikNo" disabled="@(onayci.TcKimlikNo == SelectedBirinciOnayci)">
                                                    @onayci.AdSoyad (@onayci.DepartmanAdi@(onayci.ServisAdi != null ? " - " + onayci.ServisAdi : "")) - @onayci.UnvanAdi
                                                </option>
                                            }
                                        </select>
                                        <small class="form-text text-muted">
                                            @if (AvailableApprovers.Any())
                                            {
                                                <span>@AvailableApprovers.Count adet onaycı bulundu</span>
                                            }
                                            else
                                            {
                                                <span class="text-warning">⚠️ Hiç onaycı bulunamadı</span>
                                            }
                                        </small>
                                        @if (!string.IsNullOrEmpty(SelectedBirinciOnayci) && SelectedBirinciOnayci == SelectedIkinciOnayci)
                                        {
                                            <div class="text-danger mt-1">
                                                <i class="bx bx-error-circle"></i> 1. ve 2. onaycı aynı kişi olamaz!
                                            </div>
                                        }
                                    </div>
                                }
                            }

                            <!-- Çakışma Uyarısı -->
                            @if (!string.IsNullOrEmpty(OverlapWarning))
                            {
                                <div class="alert alert-warning">
                                    <i class="bx bx-error"></i> @OverlapWarning
                                </div>
                            }

                            <!-- Buttons -->
                            <div class="d-flex gap-2">
                                <button type="button" class="btn btn-outline-secondary"
                                        @onclick="CheckOverlap" disabled="@IsChecking">
                                    @if (IsChecking)
                                    {
                                        <span class="spinner-border spinner-border-sm me-2"></span>
                                    }
                                    else
                                    {
                                        <i class="bx bx-search me-1"></i>
                                    }
                                    Çakışma Kontrolü
                                </button>
                                <button type="submit" class="btn btn-success" disabled="@IsSaving">
                                    @if (IsSaving)
                                    {
                                        <span class="spinner-border spinner-border-sm me-2"></span>
                                    }
                                    else
                                    {
                                        <i class="bx bx-save me-1"></i>
                                    }
                                    Talep Oluştur
                                </button>
                                <a href="/pdks/izin/taleplerim" class="btn btn-outline-secondary">
                                    <i class="bx bx-x me-1"></i> İptal
                                </a>
                            </div>
                        }

                    </EditForm>
                </div>
            </div>
        </div>

        <!-- Yardım Kartı -->
        <div class="col-lg-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0"><i class="bx bx-help-circle me-1"></i> Yardım</h5>
                </div>
                <div class="card-body">
                    <h6>Onay Süreci:</h6>
                    <ol class="small">
                        <li>Talep oluşturulur</li>
                        <li>1. Onaycı değerlendirir</li>
                        <li>2. Onaycı seçilmiş ise değerlendirir</li>
                        <li>Her iki onay sonrası kesinleşir</li>
                    </ol>

                    <hr />

                    <p class="small text-muted mb-0">
                        <i class="bx bx-info-circle"></i> Çakışan tarih aralıklarında talep oluşturamazsınız.
                    </p>
                </div>
            </div>
        </div>
    </div>
        </div>
    </FieldPermissionScope>
}
