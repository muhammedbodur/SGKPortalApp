<div class="card mb-4 takvim-widget-compact">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">
            <i class="bx bx-calendar me-2"></i>Takvim
        </h5>
        <a href="/common/takvim" class="btn btn-sm btn-outline-primary">
            <i class="bx bx-fullscreen me-1"></i>Tümünü Gör
        </a>
    </div>
    <div class="card-body">
        <TakvimComponent ShowSidebar="false" CalendarId="takvimWidgetCalendar" />
    </div>
</div>

<style>
    .takvim-widget-compact {
        height: 100%;
        display: flex;
        flex-direction: column;
    }

    .takvim-widget-compact .card-header {
        padding: 1rem 1rem;
        flex-shrink: 0;
    }

    .takvim-widget-compact .card-body {
        padding: 0.75rem;
        overflow-y: auto;
        flex: 1;
        min-height: 0;
    }

    /* FullCalendar kompakt görünüm */
    .takvim-widget-compact #takvimWidgetCalendar {
        width: 100%;
    }

    .takvim-widget-compact #takvimWidgetCalendar .fc {
        font-size: 0.8rem;
    }

    .takvim-widget-compact #takvimWidgetCalendar .fc-toolbar {
        margin-bottom: 0.5rem;
        gap: 0.25rem;
        flex-wrap: wrap;
    }

    .takvim-widget-compact #takvimWidgetCalendar .fc-toolbar-title {
        font-size: 0.95rem;
        font-weight: 600;
    }

    .takvim-widget-compact #takvimWidgetCalendar .fc-button {
        padding: 0.3rem 0.6rem;
        font-size: 0.75rem;
        line-height: 1;
    }

    .takvim-widget-compact #takvimWidgetCalendar .fc-button-primary:disabled {
        background-color: #e9ecef;
        border-color: #dee2e6;
    }

    /* Tablo hücreler */
    .takvim-widget-compact #takvimWidgetCalendar .fc-col-header-cell {
        padding: 0.4rem 0.25rem;
        font-size: 0.7rem;
        font-weight: 600;
    }

    .takvim-widget-compact #takvimWidgetCalendar .fc-daygrid-day-number {
        padding: 0.3rem 0.25rem;
        font-size: 0.75rem;
    }

    .takvim-widget-compact #takvimWidgetCalendar .fc-daygrid-day-frame {
        min-height: 40px;
        max-height: 50px;
    }

    .takvim-widget-compact #takvimWidgetCalendar .fc-daygrid-day-events {
        margin-top: 2px;
    }

    .takvim-widget-compact #takvimWidgetCalendar .fc-daygrid-event {
        margin-top: 1px;
        font-size: 0.65rem;
    }

    .takvim-widget-compact #takvimWidgetCalendar .fc-event-title {
        font-size: 0.65rem;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    /* Hafta sonu arka planı */
    .takvim-widget-compact #takvimWidgetCalendar .fc-daygrid-day.fc-day-sun,
    .takvim-widget-compact #takvimWidgetCalendar .fc-daygrid-day.fc-day-sat {
        background-color: #f8f9fa;
    }

    /* Scrollbar */
    .takvim-widget-compact .card-body::-webkit-scrollbar {
        width: 6px;
    }

    .takvim-widget-compact .card-body::-webkit-scrollbar-track {
        background: #f1f1f1;
        border-radius: 10px;
    }

    .takvim-widget-compact .card-body::-webkit-scrollbar-thumb {
        background: #888;
        border-radius: 10px;
    }

    .takvim-widget-compact .card-body::-webkit-scrollbar-thumb:hover {
        background: #555;
    }
</style>
